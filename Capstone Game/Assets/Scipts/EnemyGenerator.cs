using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnemyGenerator : MonoBehaviour
{
    
    // Start is called before the first frame update
    public EnemyController generateNewEnemy(Vector3 position, int points)
    {
        EnemyController newEnemy = new EnemyController();



        return newEnemy;
    }
}
